class SQLGenerationAgent:
    async def generate_sql(self, intent: dict, metadata: dict):
        # Placeholder for synthesizing SQL from parsed intent and metadata
        return "SELECT * FROM users LIMIT 10"
